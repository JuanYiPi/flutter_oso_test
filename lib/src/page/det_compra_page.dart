import 'package:flutter/material.dart';

class DetCompraPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {

    // final String idCart = ModalRoute.of(context).settings.arguments;

    return Container(
      
    );
  }
}